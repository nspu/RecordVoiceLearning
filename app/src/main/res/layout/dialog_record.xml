<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">


        <ProgressBar
            android:id="@+id/sound_pb"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="124dp"
            android:layout_height="103dp"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <ImageView
            android:id="@+id/imageView"
            android:contentDescription="@string/micro"
            android:layout_width="48dp"
            android:layout_height="62dp"
            app:layout_constraintBottom_toBottomOf="@id/sound_pb"
            app:layout_constraintLeft_toLeftOf="@id/sound_pb"
            app:layout_constraintRight_toRightOf="@id/sound_pb"
            app:layout_constraintTop_toTopOf="@id/sound_pb"
            app:srcCompat="@android:drawable/presence_audio_online" />

        <Button
            android:id="@+id/stop_btn"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"

            android:text="@string/stop"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@id/sound_pb" />


    </android.support.constraint.ConstraintLayout>

</layout>